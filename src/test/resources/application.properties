spring.profiles.active=test

# application name must be specified and be unique
spring.application.name=nextBusService

server.context-path=/nextbus
server.port=9000
management.port=9002
management.address=127.0.0.1
management.security.enabled=false
